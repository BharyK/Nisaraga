import * as React from 'react';
import { Avatar } from 'react-native-paper';

const MyComponent = () => (
  <Avatar.Text size={135} label="" color = "white" backgroundColor = "gray"/>
);

export default MyComponent